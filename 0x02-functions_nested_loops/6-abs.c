#include <stdio.h>
#include "main.h"
#include "stdlib.h"

/**
 * _abs - computes absolute value
 * @n: number to be computed
 *
 * Return: absvalue
 */

int _abs(int n)
{
	int absvalue;

	absvalue = abs(n);
	return (absvalue);
}
