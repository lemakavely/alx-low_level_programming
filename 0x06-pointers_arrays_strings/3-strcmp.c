#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strcmp - compare string to a pointer
 * @s1: destination pointer
 * @s2: source pointer
 * Return: dest pointer
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
